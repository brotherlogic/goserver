syntax = "proto3";

package goserver;

message Empty {}

message Alive {
  string name = 1;
}

message MoteRequest {
  bool master = 1;
}

service goserverService {
  rpc IsAlive (Alive) returns (Alive) {};
  rpc Mote(MoteRequest) returns (Empty) {};
}
